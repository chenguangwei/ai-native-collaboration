interface:
  display_name: "Optimize"
  short_description: "Improve interface performance across loading speed, rendering, animations, images, and bundle size. Makes experiences faster and smoother."
  brand_color: "#EC4899"
  default_prompt: "Apply the optimize workflow as defined in SKILL.md"
policy:
  allow_implicit_invocation: true
