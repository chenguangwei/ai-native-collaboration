interface:
  display_name: "Overdrive"
  short_description: "Push interfaces past conventional limits with technically ambitious implementations. Whether that's a shader, a 60fps virtual table, spring physics on a dialog, or scroll-driven reveals — make users a"
  brand_color: "#EC4899"
  default_prompt: "Apply the overdrive workflow as defined in SKILL.md"
policy:
  allow_implicit_invocation: true
