# /handoff
你现在被人类显式调用，必须立刻执行最高级别的“会话交接存档 (Handoff)”程序。
请立刻中止其他的闲聊或代码编辑操作，严格遵循以下步骤执行：

1. **环境快照审视**：通读系统上下文中最近 10-20 轮你与人类的对话，总结出“刚才完成了哪些高价值改动”、“是否遗留了尚未解决的阻碍 (Blocker)”以及“明天的下一步动作是什么”。
2. **身份自检**：读取当前系统时间戳，确立身份定位。
3. **强制覆写内存**：严格遵循 `.claude/rules/memory-sync.md` 的规范，执行 **滑动窗口压缩 (Sliding Window Compression)** 逻辑，用 `replace_file_content` 将你的交接总结覆盖写入到 `memory/handoff.md` 文件中，绝不能无脑往后追加！
4. **规范落款**：内容首部必须带上 `**[角色身份 - 年-月-日 时:分]**: ` 的前置署名。
5. **回环汇报**：物理终端写入完毕后，向人类简要汇报“已成功创建交接单”。
